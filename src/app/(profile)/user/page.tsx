import Activities from '../(component)/Activities';
import UserProfile from '../(component)/UserProfile';

const Page = () => {
  return (
    <>
      <UserProfile />
      <Activities />
    </>
  );
};

export default Page;
